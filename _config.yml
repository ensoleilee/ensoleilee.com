# Site settings
title: Ensoleilée
description: > # this means to ignore newlines until "baseurl:"
  Taiwanese-French fusion dessert innovation
baseurl: "" # the subpath of your site, e.g. /blog/
url: "http://ensoleilee.com" # the base hostname & protocol for your site

# Build settings
markdown: kramdown

collections:
  menu:
    output: true